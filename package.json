{
  "name": "tbm",
  "version": "1.0.0",
  "description": "Source for thebenmeadows.com — a static personal link hub (HTML + Tailwind).",
  "private": true,
  "dependencies": {
    "tailwindcss": "^3.4.1"
  },
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./output.css --minify"
  }
}
