{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "latest"
  }
}
